// Have a start fn that spins up web worker and gets some kind of connection to iframe
// on any message from the web worker, send it to iframe
// on any message from the iframe, send it to web worker
